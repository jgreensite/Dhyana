{
  "devDependencies": {
    "husky": "^8.0.3"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run prod"
    }
  },
  "scripts": {
    "prod": "mkdir -p dist && cp Index.html dist && cp -r Samples dist",
    "postinstall": "node ./node_modules/husky/lib/bin.js install",
    "deploy": "git push origin `git subtree split --prefix dist main`:gh-pages --force"
  }
}
